<Window x:Class="wpf.modules.DetailCustomer"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Detail Pengiriman" SizeToContent="Height" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        FontFamily="Inter, Segoe UI, Arial">

    <Window.Resources>
        <Style x:Key="CloseButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5" Padding="8">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#D32F2F"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="InfoLabelStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#5A6C7D"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="InfoValueStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="#1A1A1A"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Main Card -->
        <Border Background="White" 
                CornerRadius="15"
                Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.2" BlurRadius="30" ShadowDepth="0"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Header -->
                <Border Grid.Row="0" Background="#3B7D98" 
                        CornerRadius="15,15,0,0" 
                        Padding="30,20">
                    <Grid>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="📦 Detail Pengiriman" 
                                       FontSize="22" 
                                       FontWeight="Bold" 
                                       Foreground="White"/>
                            <TextBlock x:Name="txtIdBarang" 
                                       Text="#XXXXX" 
                                       FontSize="14" 
                                       Foreground="#E0F2F7" 
                                       Margin="0,5,0,0"/>
                        </StackPanel>

                        <!-- Close Button -->
                        <Button Content="✕" 
                                Width="35" Height="35"
                                FontSize="18"
                                FontWeight="Bold"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Style="{StaticResource CloseButtonStyle}"
                                Click="BtnClose_Click"/>
                    </Grid>
                </Border>

                <!-- Content -->
                <StackPanel Grid.Row="1" Margin="40,30">

                        <!-- Status Section -->
                        <StackPanel Margin="0,0,0,30">
                            <TextBlock Text="Status" Style="{StaticResource InfoLabelStyle}"/>
                            <Border x:Name="borderStatus"
                                    Background="#90EE90" 
                                    CornerRadius="12" 
                                    Padding="12,6" 
                                    HorizontalAlignment="Left"
                                    Margin="0,5,0,0">
                                <TextBlock x:Name="txtStatus" 
                                           Text="Pending" 
                                           FontSize="12" 
                                           FontWeight="SemiBold" 
                                           Foreground="White"/>
                            </Border>
                        </StackPanel>

                        <!-- Information Section -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Left Column -->
                            <StackPanel Grid.Column="0">
                                <!-- Nama Barang -->
                                <TextBlock Text="Nama Barang" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtNamaBarang" Text="-" Style="{StaticResource InfoValueStyle}"/>

                                <!-- Berat -->
                                <TextBlock Text="Berat" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtBerat" Text="-" Style="{StaticResource InfoValueStyle}"/>

                                <!-- Nama Kapal -->
                                <TextBlock Text="Nama Kapal" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtNamaKapal" Text="-" Style="{StaticResource InfoValueStyle}"/>

                                <!-- Lokasi Asal -->
                                <TextBlock Text="Lokasi Asal" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtLokasiAsal" Text="-" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>

                            <!-- Right Column -->
                            <StackPanel Grid.Column="2">
                                <!-- Tanggal Dikirim -->
                                <TextBlock Text="Tanggal Dikirim" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtTanggalDikirim" Text="-" Style="{StaticResource InfoValueStyle}"/>

                                <!-- Estimasi Tiba -->
                                <TextBlock Text="Estimasi Tiba" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtEstimasi" Text="-" Style="{StaticResource InfoValueStyle}"/>

                                <!-- Lokasi Tujuan -->
                                <TextBlock Text="Lokasi Tujuan" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtLokasiTujuan" Text="-" Style="{StaticResource InfoValueStyle}"/>

                                <!-- Lokasi Saat Ini -->
                                <TextBlock Text="Lokasi Saat Ini" Style="{StaticResource InfoLabelStyle}"/>
                                <TextBlock x:Name="txtLokasiSekarang" Text="-" Style="{StaticResource InfoValueStyle}"/>
                            </StackPanel>
                        </Grid>

                        <!-- Deskripsi -->
                        <TextBlock Text="Deskripsi Barang" Style="{StaticResource InfoLabelStyle}"/>
                        <Border Background="#F8F9FA" 
                                CornerRadius="8" 
                                Padding="15" 
                                Margin="0,5,0,20">
                            <TextBlock x:Name="txtDeskripsi" 
                                       Text="-" 
                                       TextWrapping="Wrap"
                                       FontSize="13"
                                       Foreground="#2C3E50"/>
                        </Border>

                        <!-- Biaya Pengiriman -->
                        <Border Background="#E8F4F8" 
                                CornerRadius="10" 
                                Padding="20" 
                                Margin="0,10,0,0">
                            <Grid>
                                <TextBlock Text="Estimasi Biaya Pengiriman" 
                                           FontSize="14" 
                                           FontWeight="SemiBold" 
                                           Foreground="#2C5F6F"
                                           VerticalAlignment="Center"/>
                                <TextBlock x:Name="txtBiaya" 
                                           Text="Rp 0" 
                                           FontSize="20" 
                                           FontWeight="Bold" 
                                           Foreground="#3B7D98"
                                           HorizontalAlignment="Right"
                                           VerticalAlignment="Center"/>
                            </Grid>
                        </Border>

                </StackPanel>

                <!-- Footer Buttons -->
                <Border Grid.Row="2" 
                        BorderBrush="#E0E0E0" 
                        BorderThickness="0,1,0,0" 
                        Padding="40,20"
                        Background="#FAFAFA"
                        CornerRadius="0,0,15,15">
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Right">
                        <Button Content="Tutup" 
                                Width="120" 
                                Height="40"
                                Background="#6C757D"
                                Foreground="White"
                                FontSize="14"
                                FontWeight="SemiBold"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="BtnClose_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="8"
                                            Padding="20,10">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#6C757D"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#5A6268"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
