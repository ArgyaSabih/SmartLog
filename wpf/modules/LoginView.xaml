<Window x:Class="wpf.modules.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf.modules"
        mc:Ignorable="d"
        Title="Login" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        
        <Style x:Key="InputBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <TextBlock x:Name="Watermark" Text="{TemplateBinding Tag}" 
                                       Foreground="Gray" VerticalAlignment="Center"
                                       Margin="2,0,0,0" IsHitTestVisible="False"/>
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="{x:Null}">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Watermark" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <TextBlock x:Name="Watermark" Text="{TemplateBinding Tag}" 
                                       Foreground="Gray" VerticalAlignment="Center"
                                       Margin="2,0,0,0" IsHitTestVisible="False"
                                       Visibility="Collapsed"/>
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" />
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Watermark" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1F4959"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#335F6F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#1A3A4A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </Window.Resources>

    <Grid>
        <Border Background="#E6F4F7" CornerRadius="15"/>

        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.8*" /> <ColumnDefinition Width="1*" />   </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#1F4959" 
                    CornerRadius="15,0,0,15">
                <StackPanel VerticalAlignment="Center" Margin="40">
                    
                    <Border Background="#335F6F" Height="200" Width="300" 
                            CornerRadius="10" HorizontalAlignment="Center">
                        <TextBlock Text="[Image Placeholder]" 
                                   Foreground="#B0C9D2" 
                                   HorizontalAlignment="Center" 
                                   VerticalAlignment="Center"
                                   FontSize="16"/>
                    </Border>
                    
                    <TextBlock Text="Seamless Shipping, Boundless Growth"
                               Foreground="White" FontSize="22" FontWeight="Bold"
                               TextWrapping="Wrap" Margin="0,30,0,0"
                               HorizontalAlignment="Center" TextAlignment="Center"/>
                    
                    <TextBlock Text="Connecting businesses worldwide through efficient and secure logistic services."
                               Foreground="#B0C9D2" FontSize="14"
                               TextWrapping="Wrap" Margin="0,15,0,0"
                               HorizontalAlignment="Center" TextAlignment="Center"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" Background="White" 
                    CornerRadius="0,15,15,0">
                <StackPanel Margin="50" VerticalAlignment="Center">
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                        <Border Background="#1F4959" Width="20" Height="20" CornerRadius="3"
                                VerticalAlignment="Center"/>
                        <TextBlock Text="SmartLogistic" FontSize="20" FontWeight="Bold"
                                   VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>

                    <TextBlock Text="Login"
                               FontSize="24" FontWeight="Bold"/>

                    <Grid Margin="0,30,0,25">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Grid.Column="0" BorderBrush="LightGray" BorderThickness="0,0,0,1" 
                                VerticalAlignment="Center" Height="1"/>
                        <TextBlock Grid.Column="1" Text="Masuk dengan email" 
                                   Foreground="Gray" Margin="10,0"/>
                        <Border Grid.Column="2" BorderBrush="LightGray" BorderThickness="0,0,0,1" 
                                VerticalAlignment="Center" Height="1"/>
                    </Grid>
                    
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="8" Padding="8" Margin="0,5,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="📧" Grid.Column="0" VerticalAlignment="Center" 
                                       FontSize="16" Margin="0,0,10,0"/>
                            <TextBox x:Name="txtEmail" Grid.Column="1" 
                                     Tag="Email" 
                                     Style="{StaticResource InputBoxStyle}"/>
                        </Grid>
                    </Border>
                    
                    <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="8" Padding="8" Margin="0,15,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔒" Grid.Column="0" VerticalAlignment="Center" 
                                       FontSize="16" Margin="0,0,10,0"/>
                            
                            <PasswordBox x:Name="txtPassword" Grid.Column="1" 
                                         Tag="Password" 
                                         Style="{StaticResource PasswordBoxStyle}"/>
                            
                            <TextBlock Text="👁️" Grid.Column="2" VerticalAlignment="Center" 
                                       FontSize="16" Cursor="Hand"/>
                        </Grid>
                    </Border>
                    
                    <TextBlock HorizontalAlignment="Right" Margin="0,15,0,0">
                        <Run Text="Tidak memiliki akun?" Foreground="Gray"/>
                        <Hyperlink Click="CreateAccount_Click" TextDecorations="None">
                            <Run Text="Buat akun" FontWeight="Bold" Foreground="#1F4959"/>
                        </Hyperlink>
                    </TextBlock>
                    
                    <Button x:Name="btnLogin" 
                            Content="Login" 
                            Margin="0,20,0,0"
                            Style="{StaticResource LoginButtonStyle}"
                            Click="BtnLogin_Click"
                            Height="40"/>
                           
                    
                    <Button x:Name="btnCancel" IsCancel="True" Click="BtnCancel_Click" 
                            Visibility="Collapsed"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>