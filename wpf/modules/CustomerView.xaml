<Window x:Class="wpf.modules.CustomerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:wpf.modules"
        mc:Ignorable="d"
        Title="SmartLogistic - Customer Dashboard" 
        WindowState="Maximized"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">

    <Window.Resources>
        <!-- Button Styles -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#335F6F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1F4959"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#335F6F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Status Badge Styles -->
        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="12,4"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#1F4959">
            <Grid Margin="30,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Logo Section -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="White" Width="35" Height="35" CornerRadius="5" Margin="0,0,15,0">
                        <TextBlock Text="📦" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <TextBlock Text="SmartLogistic" FontSize="22" FontWeight="Bold" 
                               Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Search Box removed as requested -->

                <!-- User Profile Section -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- User Avatar and Info -->
                    <Ellipse Width="40" Height="40" Fill="#3B7D98" Margin="0,0,10,0">
                        <Ellipse.Effect>
                            <DropShadowEffect BlurRadius="5" ShadowDepth="2" Opacity="0.3"/>
                        </Ellipse.Effect>
                    </Ellipse>
                    <StackPanel VerticalAlignment="Center" Margin="0,0,15,0">
                        <TextBlock x:Name="txtUserNameHeader" Text="nama" FontWeight="Bold" FontSize="14" Foreground="White"/>
                        <TextBlock x:Name="txtUserEmail" Text="nama@gmail.com" 
                                   FontSize="11" Foreground="#B0BEC5"/>
                    </StackPanel>

                    <!-- Logout Button -->
                        <Button x:Name="btnLogout" Content="Logout" 
                                Click="BtnLogout_Click"
                                Background="#DC3545" 
                                Foreground="White"
                                BorderThickness="0"
                                Padding="20,8"
                                FontSize="13"
                                FontWeight="SemiBold"
                                Cursor="Hand">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="6"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Background" Value="#DC3545"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#C82333"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="40,30,40,30">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Greeting Banner -->
            <Border Grid.Row="0" CornerRadius="12" Padding="30,20" Margin="0,0,0,20">
                <Grid>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock FontSize="28" FontWeight="Bold" Foreground="#000000">
                            <Run Text="Halo "/><Run Text="SmartLog" FontWeight="ExtraBold"/><Run Text=","/>
                        </TextBlock>
                        <TextBlock Text="Selalu Cek Barang Kamu Ya!!" FontSize="18" 
                                   Foreground="#3B7D98" Margin="0,5,0,0"/>
                    </StackPanel>
                    
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                        <!-- Filter removed per request -->
                        <Button Content="➕  Add New Export" Style="{StaticResource ActionButtonStyle}" MinWidth="130" Height="40"
                            Background="#3B7D98" Foreground="White" Padding="25,50" Click="BtnBuatPesanan_Click"/>
                        </StackPanel>
                </Grid>
            </Border>

            <!-- Top Bar with Search and Filter -->
            <Grid Grid.Row="1" Margin="0,0,0,20" Visibility="Collapsed">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Collapsed top-bar search removed -->

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="+ Buat Pesanan" Style="{StaticResource ActionButtonStyle}" 
                                Margin="0,0,10,0" Click="BtnBuatPesanan_Click" Padding="20,10" Visibility="Collapsed"/>
                        <Button Content="↻" Style="{StaticResource ActionButtonStyle}" 
                                Width="40" ToolTip="Refresh" Click="BtnRefresh_Click" Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>

                <!-- Table Container -->
                <Border Grid.Row="2" Background="White" CornerRadius="10" 
                        BorderBrush="#E0E0E0" BorderThickness="1">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <!-- Table Header -->
                        <Border Grid.Row="0" Background="#F8F9FA" 
                                CornerRadius="10,10,0,0" Padding="20,15">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Nama Barang ▼" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="1" Text="Berat ▼" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="2" Text="Kapal ▼" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="3" Text="Dibuat ▼" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="4" Text="Status ▼" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="5" Text="Estimasi ▼" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="6" Text="Detail ▼" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="7" Text="" FontWeight="SemiBold" FontSize="13"/>
                            </Grid>
                        </Border>

                        <!-- Table Content (ScrollViewer with DataGrid) -->
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                            <ItemsControl x:Name="itemsControlPengiriman" Margin="20,10">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#F0F0F0" BorderThickness="0,0,0,1" Padding="0,15">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="150"/>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="120"/>
                                                    <ColumnDefinition Width="100"/>
                                                    <ColumnDefinition Width="80"/>
                                                </Grid.ColumnDefinitions>

                                                                    <TextBlock Grid.Column="0" Text="{Binding NamaBarang}" 
                                                                               FontSize="13" VerticalAlignment="Center"/>
                                                                    <TextBlock Grid.Column="1" Text="{Binding Berat}" 
                                                                               FontSize="13" VerticalAlignment="Center"/>
                                                                    <TextBlock Grid.Column="2" Text="{Binding NamaKapal}" 
                                                                               FontSize="13" VerticalAlignment="Center"/>
                                                                    <TextBlock Grid.Column="3" Text="{Binding TanggalDibuat}" 
                                                                               FontSize="13" VerticalAlignment="Center"/>

                                                                    <!-- Status Badge -->
                                                                    <Border Grid.Column="4" Style="{StaticResource StatusBadgeStyle}"
                                                                            Background="{Binding StatusColor}">
                                                                        <TextBlock Text="{Binding Status}" FontSize="11" 
                                                                                   FontWeight="SemiBold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                                    </Border>

                                                                    <TextBlock Grid.Column="5" Text="{Binding Estimasi}" 
                                                                               FontSize="13" VerticalAlignment="Center"/>
                                                                    <TextBlock Grid.Column="6" Text="{Binding Detail}" 
                                                                               FontSize="13" VerticalAlignment="Center"/>
                                                
                                                                    <!-- Detail Button -->
                                                                    <Button Grid.Column="7" Content="📋" 
                                                                            Background="Transparent" BorderThickness="0"
                                                                            FontSize="16" Cursor="Hand"
                                                                            ToolTip="Lihat Detail"
                                                                            Click="BtnDetailPengiriman_Click"
                                                                            Tag="{Binding IdBarang}"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>

                <!-- Pagination -->
                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" 
                            Margin="0,20,0,0">
                    <Button Content="◀ Previous" Style="{StaticResource ActionButtonStyle}" 
                            Margin="0,0,10,0" Click="BtnPrevious_Click" MinWidth="80"/>
                    <Border Background="White" CornerRadius="5" Padding="15,8" 
                            BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,10,0">
                        <TextBlock x:Name="txtPageInfo" Text="01" FontWeight="SemiBold"/>
                    </Border>
                    <Border Background="White" CornerRadius="5" Padding="15,8" 
                            BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,10,0">
                        <TextBlock Text="02" Foreground="Gray"/>
                    </Border>
                    <TextBlock Text="..." VerticalAlignment="Center" Margin="5,0" FontSize="16"/>
                    <Button Content="Next ▶" Style="{StaticResource ActionButtonStyle}" 
                            Margin="10,0,0,0" Click="BtnNext_Click" MinWidth="80"/>
                </StackPanel>
            </Grid>
        </Grid>
</Window>
